const id = "compare-window";
class CompareWindow {

    /**
     * 
     * @param {Element} container
     */
    constructor(left, right, container) {
        let compWindow = document.getElementById(id);
        if (let != null) {

        }
        else {
            const el = document.createElement();
            container.appendChild(el);
            el.id = id;
            format(el);
        }
    }
}


function format(window) {
    //el.setAttribute("atr1", value);
    //element.classList.add("");
}